export const selectIsAuth = (state) => state.authorization.isAuth;
export const selectIsLoading = (state) => state.authorization.isLoading;
export const selectIsValid = (state) => state.authorization.isValid;